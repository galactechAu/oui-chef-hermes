# Host port published by Docker Compose.
OUI_CHEF_PORT=8094

# Optional: URL of a private Hermes generation bridge.
# Leave unset for shopping lists and saved meals without AI generation/import.
# Never point this at a public, unauthenticated endpoint.
# HERMES_BRIDGE_URL=http://host.docker.internal:8095/generate
